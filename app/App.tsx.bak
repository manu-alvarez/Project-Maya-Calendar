import React from 'react';
import { StyleSheet, Text, View, ScrollView, Image } from 'react-native';
import { kinCalculation } from '../src/utils/kin';
import { selloData } from '../src/utils/selloData';

export default function App() {
  // Ejemplo: calcular kin para un dato
  const kinValue = kinCalculation(selloData[0]);

  return (
    <View style={styles.container}>
      <ScrollView contentContainerStyle={styles.scrollContainer}>
        <Text style={styles.title}>Cálculo de Kin</Text>
        <Text style={styles.paragraph}>Valor de Kin: {kinValue}</Text>

        <Image
          source={require('./assets/icon.png')}
          style={styles.image}
          resizeMode="contain"
        />

        <Text style={styles.paragraph}>
          Reflexión: {selloData[0].reflexion}
        </Text>

        {/* Puedes mapear más datos si quieres */}
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingTop: 50,
  },
  scrollContainer: {
    paddingHorizontal: 20,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 12,
  },
  paragraph: {
    fontSize: 16,
    lineHeight: 22,
    marginBottom: 10,
  },
  image: {
    width: '100%',
    height: 200,
    marginBottom: 20,
  },
});
